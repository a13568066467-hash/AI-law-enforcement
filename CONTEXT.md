# AI单兵设备

本系统管理执法仪与持证巡查员的一对一绑定、人脸认证与现场 AI 能力。云端 MySQL 保存人员与设备档案；App 在本机缓存资料用于展示。

## Language

**云端档案**：
某名巡查员在 MySQL `officers` 表中的权威记录，含工号、组织信息、绑定状态与人脸模板。
_Avoid_: 云端库、数据库记录（泛指时）

**本机展示缓存**：
App 内 `OfficerProfileStore` 保存的人员摘要，仅供界面展示；内容须与云端档案一致，本身不是权威源。
_Avoid_: 本地档案、App 数据库

**初次绑定**：
持证人员在执法仪上完成注册向导（含短信与人脸），将人员与 `DSJ-` 设备编号写入云端的唯一流程；此后 App 不再作为组织信息的编辑入口。
_Avoid_: 首次注册、三步验证（仅指步骤数时）

**注销**：
在岗巡查员与本机执法仪解除绑定；云端标记离职并释放设备编号，供下一位人员初次绑定。
_Avoid_: 退出登录、logout

**退出登录**：
仅清除 App 会话 token，不改变云端绑定关系；仍可通过人脸验证重新登录同一绑定。
_Avoid_: 注销

**资料同步**：
云端档案向本机展示缓存的单向刷新；触发时机为 **App 启动（已注册且联网）**、人脸登录成功、打开「我的」页。App 不在注册/注销之外写入组织信息。
_Avoid_: 双向同步、App 改库

**绑定不一致**：
拉取云端时发现本机设备已解绑、已换人或无在岗人员，须清空本机展示缓存并引导重新初次绑定或联系管理员。
_Avoid_: 继续展示旧缓存

**报障**：
设备持有人向云端提交故障申请。**组员**须先获本组组长确认可报障；**组长**本人申报时不经「组长确认」环节，但同样须先完成现场排查并确认现有手段无法修复。
_Avoid_: 注销

**申报期间**：
**仅故障申请人**处于此状态：组长已允许其报障（或组长本人已提交）、申请在途、管理员尚未审批。此期间 **仅该申请人** 不可执勤，**同组其他人员正常作业**。
_Avoid_: 全员停岗、待工

**组长确认**（组员报障专用）：
组员在 App 发起报障前的 **一次** 前置审批：本组组长确认「已排查且无法修复，允许申报」。**不是**要求组长与组员两个职位各审批一遍，也不是组长申报时的必经步骤。
_Avoid_: 管理员审批、双人联批

**组长申报**：
组长设备故障时按与组员 **相同的排查标准**，但 **不经过「组长确认」**（避免自己批自己）；须组内互督留痕后，由组长本人提交申报。
_Avoid_: 组长确认（该词仅指组员流程）

**批准故障**：
管理员确认设备实损后的操作；申报审批通过并批准故障后，人员进入 **待工**，进入换机与解绑确认流程。
_Avoid_: 报障、申报

**解绑二次确认**：
管理员在工单中显式确认后，才正式解绑旧 `DSJ-`、更新设备故障台账。
_Avoid_: 自动注销

**误报恢复**：
管理员 **拒绝解绑**（误报或已自行恢复）后，人员恢复 **在岗**、继续使用 **原设备**；App 须 **再次人脸登录** 后方可执勤。
_Avoid_: 重新注册

**待工**：
管理员 **批准故障** 后至新执法仪 **完成初次绑定** 前的状态；期间不可执勤。新机到货后须 **完整 8 步注册绑定** 后恢复在岗。
_Avoid_: 待换机、停职
