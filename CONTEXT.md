# 赢筑AI

本系统管理公司设备池内执法仪的临时扫码占用、持证巡查员执勤会话与现场 AI 能力。云端 MySQL 保存人员与设备档案；执法仪 App 在本机缓存资料用于展示。

## Language

**云端档案**：
某名巡查员在 MySQL `officers` 表中的权威记录，含工号、组织信息、在岗状态；人脸模板由手机 App 注册时采集，执法仪不采集。表结构与「当前占用」分层见 [`docs/architecture/数据库设计.md`](docs/architecture/数据库设计.md)。
_Avoid_: 云端库、数据库记录（泛指时）

**本机展示缓存**：
App 内 `OfficerProfileStore` 保存的人员摘要，仅供界面展示；内容须与云端档案一致，本身不是权威源。
_Avoid_: 本地档案、App 数据库

**公司设备池**：
某公司在云端登记的多台 `DSJ-` 执法仪；在岗人员可选取未占用设备扫码执勤，非一人一台专属归属。
_Avoid_: 设备专属、一人一机（指长期归属时）

**设备入库**：
管理后台将执法仪登记入云端 `recorders` 表并绑定所属公司；执法仪 App 不承担入库操作。
_Avoid_: 初次绑定（旧义）、执法仪注册

**扫码绑定**：
执勤员用本仓库 `mobile-app`（手机 App）扫描执法仪「我的」页短期二维码，经云端校验后在当前设备建立当次占用会话。
_Avoid_: 人脸登录、初次绑定、8 步注册

**设备占用**：
某台执法仪当前由一名在岗人员通过扫码绑定的临时状态；一机同时最多一人，一人同时最多一机。
_Avoid_: 专属绑定、长期绑定

**解绑**：
执勤员在设置中主动结束本机与云端的当前占用，或整机断电/重启触发的等效结束；人员仍在岗，设备回到可扫码状态；保留使用历史。
_Avoid_: 退出登录、注销、logout

**使用历史**：
云端记录的每次设备占用起止（设备、人员、结束原因）；结束占用不删除历史，供对账与大屏。
_Avoid_: 当前占用、会话 token

**资料同步**：
云端档案向本机展示缓存的单向刷新；触发时机为 **扫码绑定成功**、App 启动（已占用且联网）、打开「我的」或「人员信息」页。执法仪不在绑定之外写入组织信息。
_Avoid_: 双向同步、App 改库

**绑定不一致**：
拉取云端时发现本机占用已结束、人员非在岗或公司与设备不符，须清空本机展示缓存并引导回「我的」重新扫码或联系管理员。
_Avoid_: 继续展示旧缓存

**离职注销**：
管理员或手机端将巡查员标记离职并释放其人员档案；不在执法仪设置菜单提供入口。
_Avoid_: 解绑、退出登录

**报障**：
设备当前占用人向云端提交故障申请。**组员**须先获本组组长确认可报障；**组长**本人申报时不经「组长确认」环节，但同样须先完成现场排查并确认现有手段无法修复。
_Avoid_: 解绑

**申报期间**：
**仅故障申请人**处于此状态：组长已允许其报障（或组长本人已提交）、申请在途、管理员尚未审批。此期间 **仅该申请人** 不可执勤，**同组其他人员正常作业**。
_Avoid_: 全员停岗、待工

**组长确认**（组员报障专用）：
组员在 App 发起报障前的 **一次** 前置审批：本组组长确认「已排查且无法修复，允许申报」。**不是**要求组长与组员两个职位各审批一遍，也不是组长申报时的必经步骤。
_Avoid_: 管理员审批、双人联批

**组长申报**：
组长设备故障时按与组员 **相同的排查标准**，但 **不经过「组长确认」**（避免自己批自己）；须组内互督留痕后，由组长本人提交申报。
_Avoid_: 组长确认（该词仅指组员流程）

**批准故障**：
管理员确认设备实损后的操作；申报审批通过并批准故障后，人员进入 **待工**，进入换机与解绑确认流程。
_Avoid_: 报障、申报

**解绑二次确认**：
管理员在工单中显式确认后，才正式解绑旧 `DSJ-`、更新设备故障台账。
_Avoid_: 自动离职注销

**误报恢复**：
管理员 **拒绝解绑**（误报或已自行恢复）后，人员恢复 **在岗**、继续使用 **原设备**；须在执法仪上 **再次扫码绑定** 后方可执勤。
_Avoid_: 重新注册、人脸登录

**待工**：
管理员 **批准故障** 后至在新执法仪上 **完成扫码绑定** 前的状态；期间不可执勤。
_Avoid_: 待换机、停职、8 步注册

**识图**：
对单张现场照片的云端多模态理解，结果写入会话的上次识图记忆，供后续追问。
_Avoid_: 生图、图像生成

**视频抽帧分析**：
对本机或相册中的录像按约每分钟一帧抽取 JPEG，再走与识图相同的看图模型；结果写入同一套上次识图记忆。不上传整段视频，也不使用文生视频模型。
_Avoid_: 视频生成、逐帧全量上传、VIDEO_MODEL（生成向）

**录像伴随音**：
本机循环录像写入同一片 MP4、与画面同步的现场麦克声音；分片换挡时声音不中断。与录音键产出的独立 M4A 不是同一能力。
_Avoid_: 录音（专指录音键）、推流音频、事后配音、哑录（无声 MP4）

**PTT 与录像共麦**：
循环录像期间长按 PTT：所说内容仍写入当前录像的伴随音；不得为采口语而打断或独占麦克导致片中无声。
_Avoid_: 录像中抢麦录音、先停伴随音再开 PTT

**执法仪侧键无障碍**：
息屏或 App 后台时接收机身侧键的前提能力；量产执法仪出厂默认开启，并在开机时由系统确保仍开启。App 设置内不提供跳转配置入口。
_Avoid_: 无障碍模式、无障碍自启动、系统辅助功能（泛指）

**专机桌面**：
量产执法仪开机后唯一呈现给执勤员的主控界面（本 App）；不是可选启动器，也不是普通应用列表里的一项。
_Avoid_: 开机自启动（仅指拉起进程）、默认 Home（实现词）、Launcher

**专机锁定**：
执勤员在日常使用中无法离开本 App 进入系统桌面、系统设置或其他第三方 App；维保/刷机通道不在其日常路径内。量产以 **软锁（专机桌面）+ ROM 级屏蔽系统出口** 达成；不依赖 App 内锁定任务（Lock Task）。**例外**：设备处于**未绑定人员**时，在「我的」设置中连续点击 7 次「解绑」可进入系统桌面；已占用绑定期间此出口不可用。
_Avoid_: 无法退出、全屏、kiosk（未定义时）、隐藏设置入口（未定义时）、扫码绑定入口连点（旧维保出口）

**本机相册**：
执法仪相册 Tab 统一展示本机拍摄的 JPEG 与循环录像 MP4，按时间倒序查阅；不是云端相册。
_Avoid_: 系统相册、手机相册

**画面监看**：
指挥中心在 Web 端对**已占用且在线**的执法仪**点选即开启**的独立会话：经与指挥连线相同的媒体通道仅拉取实时画面、不对讲；有独立的开始/结束信令与状态，不视为指挥连线。空闲或离线设备不建监看。换选另一台、点「停止监看」、或 Web 关页/断连时结束监看。同一设备同一时刻严格互斥：仅允许一路监看或一路指挥连线，第二座席点选则拒绝。升级为指挥连线时同房升麦/对讲、打断 AI，无语音提示。画面与本机录像共摄（未录像则自动开录）。监看阶段设备侧**不改变状态灯**、无语音提示、不打断 AI。与「直接发起连线」并存：可不经监看冷启动指挥连线。
_Avoid_: 待机画面（仅 UI 文案）、常驻预览墙、监控大屏、随时看（口语）、静默监看（无任何指示）、JPEG 预览（旧演示路径）、视频-only 指挥连线（把监看塞进连线状态机）、拆房再建升级、多路旁听监看、监看空闲超时（首期不做）、强制先监看再连线、未占用监看、监看红灯（已取消）

**指挥连线**：
指挥中心与单台执法仪之间的实时音视频会话；媒体经腾讯云 TRTC，呼叫控制由本系统自建信令完成。**进房仍仅由平台发起**、设备自动接听（含从现场事件工单上可选发起）；进房时红灯常亮、打断 AI，**无语音提示**（直接连线、监看升级与从工单发起均同）。
_Avoid_: WebRTC 专家连线（旧规划名）、视频通话（未限定方向时）、组呼、TUICallKit、来电语音播报（已取消）、设备主动进房呼叫

**现场事件工单**：
执勤员**按住 SOS 说话、松手后**经语音转写与 AI 整理生成、发往 Web 管理端的现场事件或求助单据；发送后执勤员继续执勤。工单含 AI 整理后的**正文**，并自动附带**设备、执勤人员、时间**；不是报障，也不自动开指挥连线或改写本机录制状态。Web 首期提供列表（新单置顶）、详情、状态（待处理 / 处理中 / 已关闭）及**可选发起指挥连线**；不做复杂分派与统计；列表与详情均按**本公司**边界隔离（与公司设备池一致），不做跨公司总览。SOS **短按**仍为本地**重点标记**（与工单无关）。松手后提示整理中；成功提示已上报；无有效语音或失败则**语音提示**原因且**不建空单**，可重试；**不本地排队**、不离线补传。首页「事件上报」入口暂不改。**未占用绑定**不收音并提示先扫码；**指挥连线中**不抢麦上报；**画面监看中**与**正在录像**（共麦、不中断录像）允许上报。
_Avoid_: 事件上报（口语/旧首页文案；首页入口暂不动）、SOS 应急演示、连线申请（旧设想：按下即申请通话）、报障工单、SOS 纯告警（无正文）、长按一次开关式录音（非按住）

**重点标记**：
SOS **短按**将当前录像（无则最近一条）标为本地重点文件，供本机辨认；不上报云端，也不阻止存储清理删除。
_Avoid_: 现场事件工单、云端收藏、防删保护

**连线房间**：
一次画面监看或指挥连线在 TRTC 中对应的房间；首期一房两端（1 平台座席 + 1 设备）。监看升级为连线时复用同一房间。可扩展旁听但不改变本机录像职责。
_Avoid_: 会议室、频道、channel（未定义时）

**连线信令**：
平台下发的指挥连线控制消息（呼叫开始、结束连线等）；主路径走 MQTT，HTTP 仅作兜底。设备收到呼叫后仅默认自动接听并进房，不发送应答、忙线或挂断等上行控制消息。现场事件工单的提交与状态变更不属于此类「呼叫控制」消息。不承担音视频媒体传输。
_Avoid_: 设备应答、忙线回执、设备挂断信令、TRTC 信令（泛指）、CallKit 来电信令、SDP/ICE 自建交换

**连线共摄**：
指挥连线或画面监看期间本机循环录像不中断；TRTC 画面来自与录像同源的自定义视频旁路，不得再独占打开第二路相机。
_Avoid_: TRTC 自采相机、先停录像再推流

**连线对讲**：
指挥连线中的半双工语音：设备侧按住 PTT 才上行；平台侧通过指挥台底部**广播键**开关麦做语音传呼（再点关闭）。与 AI 助手全双工 PTT 不是同一会话。
_Avoid_: 全双工对讲、AI 语音、组呼 PTT、平台常开麦（改为广播键点按开关）
